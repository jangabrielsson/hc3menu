<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- py2app bundles include a Python interpreter and bundled .so/.dylib
         files signed ad-hoc by py2app. With Hardened Runtime + notarization
         we must allow loading those: -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- PyObjC / ctypes / lupa rely on dlopen of dylibs that aren't signed
         with our team identifier. -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
